fun main(args: Array<String>) {
    print("Number range")

    for(num in 3 .. 9)
        println(num)
    for(num2 in 10 ..  20)
        println(num2)

    println("Character ranges")
    for(lett in 'c' .. 'i')
        println(lett)
}